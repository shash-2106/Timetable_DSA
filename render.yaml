services:
  - type: web
    name: timetable-system
    runtime: node
    buildCommand: npm run install:all && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
    plan: free
